# Installs do not submit the dependency graph implicitly. CI runs explicit,
# separately thresholded full-graph and production audits.
audit=false

# npm 11.18+ enforces package.json#allowScripts. Fail closed when a newly
# introduced dependency has an unreviewed install-time lifecycle script.
strict-allow-scripts=true
